# UNUSED / EXAMPLE ONLY
This worker is scaffold/example code and is not actually deployed to production.
Do not mistake it for the production backend. The real API backend is served via Pages Functions in `functions/api/[[path]].ts`.
